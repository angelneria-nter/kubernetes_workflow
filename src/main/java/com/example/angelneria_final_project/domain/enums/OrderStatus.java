package com.example.angelneria_final_project.domain.enums;

public enum OrderStatus {
    PENDING, PROCESSING, READY_TO_SHIP, SHIPPED, DELIVERED,
    CANCELLED, RETURN_REQUESTED, RETURNED, REFUNDED, PAYMENT_FAILED
}
